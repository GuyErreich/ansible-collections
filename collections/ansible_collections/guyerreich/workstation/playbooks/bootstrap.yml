---
# Bootstrap a local workstation (macOS, Ubuntu, or WSL).
#
# Install from Galaxy, then run by playbook FQCN (no git clone required):
#   ansible-galaxy collection install guyerreich.workstation
#   ansible-playbook guyerreich.workstation.bootstrap
#
# Non-interactive (example):
#   ansible-playbook guyerreich.workstation.bootstrap \
#     -e onepassword_skip_guide=true \
#     -e @examples/extra-vars.yml
#
# Partial re-run:
#   ansible-playbook guyerreich.workstation.bootstrap --tags shell,git

- name: Bootstrap workstation
  hosts: localhost
  connection: local
  gather_facts: true
  roles:
    - role: guyerreich.workstation.platform
      tags:
        - always
        - platform
    - role: guyerreich.workstation.brew
      tags:
        - brew
    - role: guyerreich.workstation.github_cli
      tags:
        - gh
        - github_cli
    - role: guyerreich.workstation.onepassword
      tags:
        - 1password
        - onepassword
    - role: guyerreich.workstation.gitconfig
      tags:
        - git
        - gitconfig
    - role: guyerreich.workstation.shell
      tags:
        - shell
    - role: guyerreich.workstation.verify
      tags:
        - verify
